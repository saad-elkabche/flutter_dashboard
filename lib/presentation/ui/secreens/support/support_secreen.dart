import 'package:flutter/material.dart';




class SupportSecreen extends StatelessWidget {

  SupportSecreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('support'),
    );
  }
}
