import 'package:flutter/material.dart';




class LogoutSecreen extends StatelessWidget {

  LogoutSecreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('logout'),
    );
  }
}
