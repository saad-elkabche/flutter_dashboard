import 'package:flutter/material.dart';




class WithDrawSecreen extends StatelessWidget {

  WithDrawSecreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('withdraw'),
    );
  }
}
