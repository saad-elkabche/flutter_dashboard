import 'package:flutter/material.dart';




class PlanSecreen extends StatelessWidget {

  PlanSecreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('plan'),
    );
  }
}
